# Build results
bin/
obj/
*.user

# IDE
.vs/
.idea/
*.suo
*.userosscache

# Runtime
appsettings.Production.json
*.log

# ===== BẢO MẬT: không commit file chứa bí mật =====
# admin-config.json chứa API key LLM cá nhân (do user nhập ở trang Settings)
admin-config.json

# appsettings.json chứa connection string MongoDB (có password) + master password admin.
# KHÔNG commit file này — dùng env vars trên Render (dạng Key__SubKey, vd ConnectionStrings__MongoDb).
appsettings.json
